const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

  return message.channel.send('loadingmenu, area51, sheepbotsucks, and afk using the ^ prefix.');

};

module.exports.help = {
  name: "commands"
};